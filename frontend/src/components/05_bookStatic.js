import React from 'react';

function BookStatic(){

    //ISBN 서지(도서분류번호)를 이용

    //DB에서 date.month에 해당하는 bookCount를 받기 + 각 책의 장르도 받기
    return(
        "bookStatic"
    )
}

export default BookStatic;